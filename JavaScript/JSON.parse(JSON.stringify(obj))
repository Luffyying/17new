一、基本概念

由js中数据类型分为两类：基本数据类型（Undefined Null Boolean Number String）和应用类型（Object Array Function等）

数据是存在内存中的，且内存中包括了变量的标识和值

这些基本数据类型放在栈中（栈存放的都是些简单的数据段，数据大小确实，内存空间自动分配）

那些引用类型一般是存放在堆中（值的大小不确定）,但是变量对象实际上是存在栈中的指针(可以理解为标识)，指向真正的堆数据

故：
var a = {b:7}
var c = a;

的过程就是在栈中新开辟了，但是两个标识指向的是同一块堆内存。

二、拷贝

深浅拷贝是针对应用类型来说的。

深拷贝：另开辟一段内存空间（主要是说堆内存），与拷贝的原对象脱离关系，改变值不影响现对象。

浅拷贝：和深拷贝相对，指向同一个引用

要点：用JSON.parse(JSON.stringify(obj))来进行深拷贝

（1）var a = JSON.stringify(obj)

将js对象转换为字符串对象，已然obj和a已经没啥关系了

 (2) var b = JSON.parse(a)

此时得到的就是深拷贝的结果
